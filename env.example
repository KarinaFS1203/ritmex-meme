# Four Meme CLI Dashboard Configuration

# Default sort mode (Hot, TimeDesc, OrderDesc, ProgressDesc)
DEFAULT_SORT=Hot

# Default filter mode (all, chinese, bsc)
DEFAULT_FILTER=all

# Refresh interval in milliseconds
REFRESH_INTERVAL=1000

# Number of tokens to fetch per page
PAGE_SIZE=30
